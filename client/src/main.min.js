import Vue from 'vue'; import App from './App.vue'; import router from './router'; import store from './store'; Vue.config.productionTip = !1; export const bus = new Vue(); new Vue({ router: router, store: store, render: h => h(App) }).$mount('#app')
